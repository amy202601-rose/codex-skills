interface:
  display_name: "WFS Marketing PB CN"
  short_description: "把中文口播稿生成发布字段并写入加拿大Wallace发布中心"
  default_prompt: "Use $wfs-marketing-pb-cn to turn this Chinese oral script into publish-ready fields and write it into 加拿大Wallace."
